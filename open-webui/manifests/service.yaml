apiVersion: v1
kind: Service
metadata:
  name: open-webui-svc
spec:
  selector:
    app: open-webui
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30353
  type: NodePort
